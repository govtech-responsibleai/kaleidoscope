import {
  IconSettings,
  IconRocket,
  IconBook,
} from '@tabler/icons-react';

export const sidebarIcons: Record<string, React.ComponentType<{size?: number; stroke?: number}>> = {
  IconSettings,
  IconRocket,
  IconBook,
};
